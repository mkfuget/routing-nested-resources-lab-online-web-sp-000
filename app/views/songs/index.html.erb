<ul>
<% @songs.each do |song| %>
  <li><%= ArtistsHelper.display_artist(song) %> - <%= link_to song.title, artist_songs_path(song) %></li>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
